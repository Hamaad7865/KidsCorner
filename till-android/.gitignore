local.properties
.gradle/
build/
*.iml
.idea/
.kotlin/

# The release signing key's passwords. The .jks file itself is kept entirely
# outside this repo (see docs/) — this is only the pointer to it, but it is
# just as sensitive: whoever has both can sign an update Android will accept
# as this app.
app/keystore.properties
