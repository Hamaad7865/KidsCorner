org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# The till's API origin (its own /api/till routes). Points every build — debug
# APKs included — at the live deployment, so a tablet works out of the box.
# For emulator work against `npm run dev`, override on the command line with
# `-PapiOrigin=http://10.0.2.2:3001` or comment this out.
apiOrigin=https://kidscorner.boodoo-sheik786.workers.dev
